# PNPM / NPM configuration
# auto-install peers disabled to avoid unexpected peer installs
auto-install-peers=false
# allow installing packages with lifecycle/build scripts when necessary
# WARNING: enabling lifecycle scripts can be a supply-chain risk. Only enable in trusted environments.
ignore-scripts=false
# loosen peer dependency checks (keeps install from failing on peer mismatches)
strict-peer-dependencies=false
